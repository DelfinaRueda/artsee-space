<% if artist.photo.attached? %>
  <%= cl_image_tag artist.photo.key, crop: :fill, class: "avatar" %>
<% else %>
  <%= cl_image_tag artist.profile_pic, :width=>300, :crop=>"scale", class: "avatar" %>

  <%# <img class="avatar" src="<>% = artist.profile_pic %<> ?w=500&fit=max"> %>
<% end %>
