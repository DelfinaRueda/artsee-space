<% if artist.photo.attached? %>
  <%= cl_image_tag(artist.profile_pic, :width => 400, :crop => :fill) %>
<% else %>
  <%= cl_image_tag(artist.profile_pic, :width => 400, :crop => :fill) %>


<% end %>
